# Contributing - Web

Same workflow as the project convention. See root CONTRIBUTING.md.

## Before Pushing

- [ ] Build passes (`npm run build`)
- [ ] Lint passes (`npm run lint`)
- [ ] TypeScript check passes (`npm run typecheck`)
- [ ] No console.log or debug code
